<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.okerry.backend.dao.AuthServiceDAO">

    <insert id="insertAuthCode" parameterType="map">
        INSERT INTO mail_auth (target_email, code, expired_date, auth_yn)
        VALUES (#{email}, #{code}, NOW() + INTERVAL 5 MINUTE, 'N');
    </insert>

</mapper>
